<h3> <%= @markerName %> </h3>
<button class="btn" onclick="location.href='/login/show'">Logout</button>
<table class="striped">
    <thead>
        <tr style="">
            <th> <i class="material-icons">assignment</i>   Courses </th>
        </tr>
    </thead>
    <tbody>
        <% @courses.each do |course| %>
            <tr>
                <td> <%= link_to course.long_title, marker_course_path(@marker, course) %> </td>
            </tr>
        <% end %>
    </tbody>

</table>